// val num: Int = 123

val Int = class -> {
    public var value: i32

    val Int = (arg: i32) -> value = arg

    val Int = (arg: String) -> {
        for (var i in arg.lentgth) {
            // TO-DO
        }
    }

    val toString: () -> String(value)
    val toFloat: () -> Float(value)
    val toDouble: () -> Double(value)
}


val string = class -> {
    private var ch: vector(char)
    public var length: i64 = 0

    public val string = (arg: vector(char)) -> ch = arg
    
}