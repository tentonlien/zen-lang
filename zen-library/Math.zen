trait Math {
		def abs(i64)
		def calcBigNumber()
    def pow(i64, i64)
}

class Math {
    def abs(val: i64) -> val > 0 ? val : 0 - val
    
    def calcBigNumber() {}

    def pow(num: i64, times: i64) {}
}